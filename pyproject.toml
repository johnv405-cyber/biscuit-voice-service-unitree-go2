[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "biscuit-voice-service-unitree-go2"
version = "0.1.0"
description = "Voice and interaction system for the Unitree Go2 robot"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools]
py-modules = ["voice_service", "sensor_monitor", "example_integration"]
